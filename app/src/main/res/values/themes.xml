<resources>
    <!-- Base application theme. -->
    <style name="Theme.Attempt3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fallback for API < 27 where dark icons are not supported. 
             Using a semi-transparent background ensures white icons are visible. -->
        <item name="android:navigationBarColor">#80000000</item>
    </style>

    <!-- Splash Screen Theme. -->
</resources>